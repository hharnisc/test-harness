

describe 'My Ctrls', ->
  before ->
  beforeEach ->

  it 'textbox', -> @insert(Ctrl.defs.foo)

  it 'spinner', ->
    console.log 'RUN spinner', @

  describe 'sub-set', ->

  it 'column-set', ->
    console.log 'RUN column-set', @



